/* polish.css — minimal, tasteful defaults */
:root{
  --bg:#0b0b0c; --fg:#e9eaee; --muted:#a9acb6; --ink:#111216; --radius:16px; --container:1100px;
}
html{font-family:Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial; color:var(--ink)}
body{margin:0; background:#fff; color:var(--ink)}
.container{max-width:var(--container); margin:0 auto; padding:20px}
.h1{font-size:clamp(28px, 2.2vw + 18px, 38px); line-height:1.15; margin:0 0 8px}
.muted{color:var(--muted)}
.button{display:inline-block; padding:10px 14px; border-radius:12px; background:#111; color:#fff; text-decoration:none}
.button:hover{filter:brightness(1.08)}
.card{background:#fff; border:1px solid #e9e9ee; border-radius:var(--radius); padding:18px; box-shadow:0 8px 20px rgba(17,18,22,0.04)}
@media (max-width:720px){ .container{padding:14px} }
